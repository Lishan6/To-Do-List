import{TASK_INPUT_ID, TASK_LIST_CLASS } from './constant.js';
import { renderTask } from './renderTask.js';

 export  function addTask(event){   //function add new task
    event.preventDefault(); //to prevent the page from reloading on form submission
    const inputValue=input.value; //get the value of the input field
    console.log(inputValue);
    
    
}
        